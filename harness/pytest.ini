[pytest]
markers =
    automation: drives a real Godot client window via the debug automation
        hook (client/scripts/automation_server.gd + automation.py). Needs a
        real display and is slow (real Godot boot); excluded by default via
        the addopts below. Run explicitly with `-m automation`.
addopts = -m "not automation"
